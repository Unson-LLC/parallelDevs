// constants.js - ゲーム全体で使用する定数の定義

// ゲーム領域のサイズ
const GAME_WIDTH = 800;
const GAME_HEIGHT = 600;

// パドルのサイズ
const PADDLE_WIDTH = 100;
const PADDLE_HEIGHT = 20;

// ボールのサイズ
const BALL_SIZE = 10;

// ブロックのサイズと配置
const BLOCK_WIDTH = 75;
const BLOCK_HEIGHT = 20;
const BLOCK_ROWS = 5;
const BLOCK_COLS = 10;

// 名前付きエクスポート（export defaultは使用しない）
export { 
  GAME_WIDTH, 
  GAME_HEIGHT, 
  PADDLE_WIDTH, 
  PADDLE_HEIGHT, 
  BALL_SIZE, 
  BLOCK_WIDTH, 
  BLOCK_HEIGHT, 
  BLOCK_ROWS, 
  BLOCK_COLS 
};